# .env.example - Do not commit the actual .env file to git

# A secure, randomly generated key for signing JWT tokens
JWT_SECRET=your_super_secret_random_key_here

# The path to the SQLite database file.
# If not specified, it defaults to 'cms.db' in the custom-cms directory.
DATABASE_PATH=./cms.db
